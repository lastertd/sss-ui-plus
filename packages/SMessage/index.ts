import "./m.less"
import message from "./message";
import notify from "./notify";
import confirm from "./confirm";

export default {
    message,
    notify,
    confirm
}

export {
    message,
    notify,
    confirm
}