import "./m.less"
import message from "./message.ts";
import notify from "./notify.ts";
import confirm from "./confirm.ts";

export default {
    message,
    notify,
    confirm
}

export {
    message,
    notify,
    confirm
}